/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Expose NEXT_PUBLIC_ vars from .env at build time.
  // No extra configuration needed — Next.js handles these automatically.
};

module.exports = nextConfig;
